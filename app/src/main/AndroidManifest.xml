<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="amirahmed.com.mtlf4androidapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".Utils.SplashScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.LoginActivity" />

        <activity android:name=".Activities.RegistrationActivity"></activity>
        <activity android:name=".Activities.ShopPageTwo"></activity>
        <activity android:name=".Activities.ShopPageThree"></activity>
        <activity android:name=".Activities.ShopPageFour"></activity>
        <activity android:name=".Activities.ProfileActivity"></activity>
        <activity android:name=".Activities.FilterActivity"></activity>
        <activity android:name=".Activities.MyProfileActivity"></activity>
        <activity android:name=".Activities.MyProfileSetting"></activity>
        <activity android:name=".Activities.AddPost1"></activity>
        <activity android:name=".Activities.AddPost2"></activity>
        <activity android:name=".Activities.LoginCompany"></activity>
        <activity android:name=".Activities.StatsticsActivity"></activity>
        <activity android:name=".Activities.FavoritesActivity"></activity>
    </application>

</manifest>